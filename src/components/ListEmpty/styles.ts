
import { styled } from "nativewind";
import { Text, View } from "react-native";

export const Container = styled(View, "flex-1 justify-center items-center")

const titleStyles = "text-center text-sm font-normal text-gray-300"
export const Title = styled(Text, titleStyles)

